{
  "buildCommand": "npm run build --workspace=packages/client",
  "outputDirectory": "packages/client/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
