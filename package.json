{
  "name": "botville",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "pretest": "npm run build --workspace=packages/shared && npm run fixture --if-present",
    "test": "node --import ./test/ts-resolve.mjs --test --test-concurrency=4 --test-reporter=spec \"test/*.test.mjs\" \"test/*.test.ts\" && npm run golden:names",
    "test:bake": "npm run pretest && node --import ./test/ts-resolve.mjs --test --test-concurrency=2 \"test/bake/**/*.test.mjs\" \"test/bake/**/*.test.ts\"",
    "test:all": "npm test && npm run test:bake && node -e \"const d=require('child_process').execSync('git status --porcelain',{encoding:'utf8'}).split('\\n').filter(Boolean);if(d.length){console.error('tests left the tree dirty (modified or untracked):\\n'+d.join('\\n'));process.exit(1)}\"",
    "golden:names": "node scripts/snapshot-legacy-names.mjs",
    "pack:index": "node scripts/index-pack.mjs",
    "pin": "node scripts/pin.mjs",
    "contact": "node scripts/contact-sheet.mjs",
    "fixture": "node scripts/gen-fixture-pack.mjs && node scripts/pin.mjs fixture test/fixtures/pack-src",
    "validate:contract": "node scripts/validate-contract.mjs",
    "bake:world": "node scripts/world-bake.mjs",
    "dev:server": "npm run dev --workspace=packages/server",
    "dev:client": "npm run dev --workspace=packages/client",
    "deploy:client": "node scripts/sync-assets.mjs && vercel pull --yes --environment=production && vercel build --prod && vercel deploy --prebuilt --prod",
    "deploy:server": "node scripts/deploy-server.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "puppeteer-core": "^25.3.0",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "npm@11.17.0"
}
