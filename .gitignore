# Dependencies
node_modules/

# Build output
dist/
.turbo/
*.tsbuildinfo

# Secrets — only *.example templates are committed
.env
.env.*
!.env.example
!.env.production.example

# Local database
*.db
*.db-shm
*.db-wal

# Paid art — licence permits use, forbids redistribution (see docs/ASSETS.md)
# No trailing slash: some dev setups symlink this in (a trailing-slash
# pattern does not match a symlink-to-directory, only a real directory).
assets-src
packages/client/public/assets/tilesets/pack/
packages/client/public/assets/sprites/pack/
packages/client/public/assets/ui/pack/
packages/client/public/assets/baked/
# ...and the vendor-named equivalents the FROZEN legacy pipeline
# (test/golden/legacy/, driven by scripts/capture-golden-baseline.mjs) still
# writes real licensed pixels to — these lost their ignore coverage in the
# limezu->pack rename (the new sync-assets.mjs/world-bake write only to the
# pack/ paths above) but the frozen scripts never got the memo, since
# freezing them is the point (Task 19).
packages/client/public/assets/tilesets/limezu/
packages/client/public/assets/sprites/limezu/
packages/client/public/assets/ui/limezu/

# Deploy remote config (written by scripts/deploy-server.mjs)
.deploy-server.json

# Editor / OS
.DS_Store
.vscode/
.idea/

# Internal tooling
.claude/

# Full per-cell pack inventory — regenerable browsing aid (npm run pack:index).
# The small sheets manifest beside it IS committed: its diff is how a pack
# update announces itself.
sources/*.index.json

# Generated synthetic test pack (npm run fixture) — pixels are derived, not authored
test/fixtures/pack-src/

# Contact sheets — regenerable review artifacts (npm run contact). They render
# licensed pixels when built against a real pack, so they never enter git.
contact/
